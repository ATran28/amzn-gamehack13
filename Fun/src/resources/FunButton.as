package resources
{
	import starling.display.Button;
	import starling.textures.Texture;
	
	public class FunButton extends Button
	{
		public function FunButton(upState:Texture, text:String="", downState:Texture=null)
		{
		}
	}
}